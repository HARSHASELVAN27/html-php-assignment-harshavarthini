<!DOCTYPE html>
<html>
<head>
    <title>Purchase Calculator</title>
</head>
<body>

<h2>Purchase Calculator</h2>

<form method="post">
    <h3>Item 1</h3>
    Price: <input type="number" step="0.01" name="price1" required><br>
    Quantity: <input type="number" name="qty1" required><br>

    <h3>Item 2</h3>
    Price: <input type="number" step="0.01" name="price2" required><br>
    Quantity: <input type="number" name="qty2" required><br>

    <h3>Item 3</h3>
    Price: <input type="number" step="0.01" name="price3" required><br>
    Quantity: <input type="number" name="qty3" required><br><br>

    <input type="submit" value="Calculate">
</form>

<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // Get values from form
    $price1 = $_POST['price1'];
    $qty1   = $_POST['qty1'];

    $price2 = $_POST['price2'];
    $qty2   = $_POST['qty2'];

    $price3 = $_POST['price3'];
    $qty3   = $_POST['qty3'];

    // Calculate subtotal
    $subtotal = ($price1 * $qty1) + ($price2 * $qty2) + ($price3 * $qty3);

    // Calculate tax (10%)
    $tax = $subtotal * 0.10;

    // Calculate total cost
    $total = $subtotal + $tax;

    // Display results
    echo "<h3>Result:</h3>";
    echo "Subtotal: $" . number_format($subtotal, 2) . "<br>";
    echo "Tax (10%): $" . number_format($tax, 2) . "<br>";
    echo "Total Cost: $" . number_format($total, 2) . "<br>";
}
?>

</body>
</html>

output:

Purchase Calculator
Item 1
Price: 

Quantity: 

Item 2
Price: 

Quantity: 

Item 3
Price: 

Quantity: 


Result:"; echo "Subtotal: $" . number_format($subtotal, 2) . "
"; echo "Tax (10%): $" . number_format($tax, 2) . "
"; echo "Total Cost: $" . number_format($total, 2) . "
"; } ?>